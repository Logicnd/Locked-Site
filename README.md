# Locked Site — Minimal Static Start

This repository has been reset to a minimal static site (HTML/CSS/JS).

To commit changes from this machine, run the included PowerShell helper:

```powershell
.\commit.ps1 -Message "Your commit message here"
```

No server or Node.js is required for this static site.
