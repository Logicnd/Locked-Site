# Example environment variables. Copy to `.env` and fill in values.

SITE_TITLE=Locked Site — Local
# ANON_SALT: a random secret used for client-side anonymization (keep private)
ANON_SALT=your-secret-salt-here
ANALYTICS_ENABLED=false
PORT=3000
